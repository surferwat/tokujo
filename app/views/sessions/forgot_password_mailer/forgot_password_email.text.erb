Hi <% if @user.username.blank? %>friend<% else %><%= @user.username %><% end %>

Can't remember your password? No worries. Click the link below and follow the instructions to set a new password.

<%= @url %>