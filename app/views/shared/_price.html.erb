<%# Price should be a formatted Money object. %>
<span>
    <span><%= price %></span>
    <% if with_tax_text %>
      <span>&nbsp(<%= t("views.shared._price.incl_tax")%>)</span>
    <% end %>
</span> 