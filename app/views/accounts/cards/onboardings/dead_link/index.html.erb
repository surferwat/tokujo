<% content_for :title do %>
  Onboarding Process Failed
<% end %>

<div class="mt-8 min-h-full flex flex-col justify-center lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-lg">
    <div class="space-y-6">
      <div class="flex items-center space-x-3">
        <p class="text-base">The onboarding process failed. Please try again.</p>
      </div>
      <%= render partial: "shared/button_solid", locals: { text: "Go onboard", href: accounts_cards_onboardings_path } %> 
    </div>
  </div>
</div> 